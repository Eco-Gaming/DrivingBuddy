import 'package:flutter/material.dart';

class GasLogPage extends StatelessWidget {
  const GasLogPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
        child: Text('Gas Log Page')
    );
  }
}