import 'package:flutter/material.dart';

class DrivingLogPage extends StatelessWidget {
  const DrivingLogPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
        child: Text('Driving Log Page')
    );
  }
}